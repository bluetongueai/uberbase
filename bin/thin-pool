#! /bin/bash

# 10gb
dd if=/dev/zero of=${PWD}/data/lvm bs=1024 count=10485760
lo_dev=$(losetup -f)
losetup $lo_dev ${PWD}/data/lvm
vgcreate vg0 $lo_dev
modprobe dm_thin_pool
lvcreate -L 9GB --type thin-pool --thinpool containerd-thinpool vg0
