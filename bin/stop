#! /bin/bash

# stop all containers
podman stop -a

# remove all containers
podman container prune -f

# prune all volumes
podman system prune -f

echo -e "\033[33mAll Uberbase services stopped and removed.\033[0m"
